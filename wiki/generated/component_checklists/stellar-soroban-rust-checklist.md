# Stellar/Soroban Rust 合约审计入口 checklist，覆盖 require_auth、storage TTL/rent、Vec 输入唯一性、整数 fixed-point math、emission/index 更新顺序、pool status 与 vault/auction accounting。

> Generated by `python -m audit_rag.cli.main export-wiki`. Do not edit this page by hand; update `data/normalized/` instead.

## Metadata

- id: `stellar-soroban-rust-checklist`
- document_type: `component-checklist`
- source_json: `data/normalized/component_checklists/stellar-soroban-rust-checklist.json`
- component_type: stellar-soroban-rust-contract

## Summary

Stellar/Soroban Rust 合约审计入口 checklist，覆盖 require_auth、storage TTL/rent、Vec 输入唯一性、整数 fixed-point math、emission/index 更新顺序、pool status 与 vault/auction accounting。

## Tags

- stellar-rust
- soroban-rust
- require-auth
- storage-ttl
- integer-math
- duplicate-assets
- emissions

## Related pattern ids

- soroban-require-auth-entrypoint-bypass-pattern
- soroban-storage-ttl-rent-dos-pattern
- soroban-duplicate-asset-list-pricing-pattern
- soroban-emission-accrual-before-state-change-pattern
- soroban-integer-math-low-denominator-pattern

## Check items

- 列出所有 pub fn / #[contractimpl] entrypoint，标记 value-moving、config-changing、claiming 三类敏感动作
- 对每个 Address 参数确认是否 require_auth，以及 require_auth 的主体是否等于被扣款/被 claim 的账户
- 搜索 storage().temporary/persistent/instance，确认关键状态 TTL extension、上限和 cleanup
- 对 Vec<Address>/Vec<Asset>/reserve list 写重复项测试，比较 [A] 与 [A,A] 结果
- 对 reward/emission/interest index 检查 update-before-mutate 顺序
- 对 total_supply、denominator、rate、fixed-point math 做 0/1/min/max 边界测试
- 检查 flash-loan、auction、withdraw、donate/draw 是否绕过 pool status/freeze/backstop 状态检查

## Test ideas

- Soroban unit test：随机 actor/owner/receiver 组合，断言未授权账户无法 claim/withdraw/config
- proptest：任意 Vec 输入先 dedupe 后的估值应等于协议计算值
- ledger/time simulation：storage TTL 过期后 withdrawal/claim 不能丢失关键状态
- boundary math：low supply、zero denominator、donation/share inflation、rounding repeatability

## Navigation

- Generated index: [[generated/index|Generated normalized index]]
- Suggested concept page: [[concepts/soroban-require-auth|Soroban require_auth]]
